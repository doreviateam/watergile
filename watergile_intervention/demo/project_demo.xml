<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Projet Agrosys ERP -->
        <record id="project_agrosys_erp" model="project.project">
            <field name="name">AgroSys ERP</field>
            <field name="partner_id" ref="watergile_intervention.client_la_platine"/>
            <field name="description">Implémentation Odoo 17 pour La Platine</field>
            <field name="privacy_visibility">portal</field>
            <field name="date_start" eval="(datetime.now()).strftime('%Y-%m-%d')"/>
            <field name="date" eval="(datetime.now() + relativedelta(months=6)).strftime('%Y-%m-%d')"/>
            <field name="active" eval="True"/>
            <field name="color">3</field>  <!-- Bleu -->
            <field name="is_watergile" eval="True"/>  <!-- C'est un projet Watergile -->       
            
        </record>
    </data>
</odoo>